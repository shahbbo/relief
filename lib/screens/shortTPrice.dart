import 'package:flutter/material.dart';

class shortTPrice extends StatelessWidget {
  shortTPrice({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('shortTPrice'),
    );
  }
}
