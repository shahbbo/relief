import 'package:flutter/material.dart';

class liveInPrice extends StatelessWidget {
  liveInPrice({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('liveInPrice'),
    );
  }
}
