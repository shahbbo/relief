import 'package:flutter/material.dart';

class AboutCare extends StatelessWidget {
  const AboutCare({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
